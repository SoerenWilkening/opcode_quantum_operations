# K05 — casts (`sext` / `zext` / `trunc`, int↔int)

**Bennett source:** `third_party/bennett/src/lowering/arith.jl` lines 534-566 (pinned commit 980805de8531…)
**libcqops module:** M13 `kernels/cast.c`, 90 LOC budget, no split seam — plan §3 Layer 3, "| M13 | `kernels/cast.[ch]`" (IMPLEMENTATION_PLAN.md:1166 @ 961905f)
**Clean or sandwich:** **clean** — there is no compute half to sandwich.

> **Citation form — converted 2026-09-10 (`bd 0a7`).** This file's citations into
> *living* documents (`PRD-v1.md`, `IMPLEMENTATION_PLAN.md`) are now `§` + a
> `grep -nF`-unique quoted phrase + `file:line @ <sha>`. Measured: a re-measured
> `PRD-v1.md` line number survives about three commits, and four earlier re-measure
> cycles across these K-docs all went stale again. **Verify by grepping the phrase in
> the current file**; recover the exact original with
> `git show <sha>:<file> | sed -n <N>p`. Citations into `third_party/` keep bare line
> numbers — that tree is pinned and never moves.

`lower_cast!` allocates exactly one wire vector, `r = allocate!(wa, T)` (arith.jl:540), and
that vector **is the result**, not scratch. Every gate emitted in all three branches targets
`r[i]`; the source `src` appears only as a CNOT *control* and is never a target. So the
construction allocates zero scratch and therefore leaves zero scratch dirty. There is nothing
for `cq_sandwich` to reverse.

This matches PRD-v1.md §5 ("Naturally clean already (no sandwich needed): `and`, `or`, `xor`,
constant `shl/lshr/ashr`, `sext/zext/trunc`") and the PRD §6 K5 row (`✔`, `≤ T CNOT`). Both
claims hold against the source — see §3.

> **Do not wrap K5 in `cq_sandwich`.** It would still be *correct* (compute `scratch ^= a`,
> copy out `dst ^= scratch`, uncompute `scratch ^= a`), but it costs **3×** the CX and burns
> `F` ancillae to achieve exactly what one CX per bit already achieves. The `✔` in the
> catalogue is load-bearing.

---

## 1. Julia source excerpt

`third_party/bennett/src/lowering/arith.jl`, lines 534-566, verbatim:

```julia
# ---- casts (sext, zext, trunc) ----

function lower_cast!(gates, wa, vw, inst::IRCast)
    src = resolve!(gates, wa, vw, inst.operand, inst.from_width)
    F = inst.from_width
    T = inst.to_width
    r = allocate!(wa, T)

    if inst.op == :zext
        for i in 1:F; push!(gates, CNOTGate(src[i], r[i])); end
    elseif inst.op == :sext
        for i in 1:F; push!(gates, CNOTGate(src[i], r[i])); end
        for i in F+1:T; push!(gates, CNOTGate(src[F], r[i])); end
    elseif inst.op == :trunc
        # Bennett-gboa / U139 wire-state contract:
        #   - r[1:T] ← src[1:T] (CNOT-copy of the low T bits).
        #   - src[T+1..F] are NOT touched: they remain at their SSA-input
        #     values for the rest of the gate sequence. This is intentional
        #     — `src` is a pure SSA read, not consumed. Bennett's outer
        #     reverse pass uncomputes whatever produced src in the first
        #     place; the high bits never need explicit zeroing here.
        #   - r is freshly allocated (zero-initialised), so the result wires
        #     don't have a "dirty" issue.
        # If a future liveness pass wants to free src mid-circuit, it must
        # first uncompute src's full F-bit producer — NOT just bits 1..T.
        # Pinned by `test/test_gboa_dirty_bit_hygiene.jl`.
        for i in 1:T; push!(gates, CNOTGate(src[i], r[i])); end
    else
        throw(ArgumentError("lower_cast!: unknown cast op :$(inst.op) (supported: $_IR_CAST_OPS)"))
    end

    vw[inst.dest] = r
end
```

Supporting reads:

| Fact | Where |
|---|---|
| `_IR_CAST_OPS == (:sext, :zext, :trunc)` — no other int cast exists | `src/ir_types.jl:11` |
| `IRCast` fields `dest, op, operand, from_width, to_width`; constructor checks only `op ∈ _IR_CAST_OPS` and both widths `≥ 1` — **no `F ≤ T` / `T ≤ F` guard** | `src/ir_types.jl:200-216` |
| Dispatched by `_lower_inst!(ctx, inst::IRCast, ::Symbol)` — one entry point, no strategy kwarg, no `:auto` variant | `src/lowering/types.jl:253-254` |
| LLVM `SExt`/`ZExt`/`Trunc` → `IRCast(dest, opname, …, _iwidth(src), _iwidth(inst))` | `src/extract/instructions.jl:6290-6294` |
| `allocate!(wa, n)` hands out `n` fresh wires (or wires off the free list, which the allocator's contract states "MUST be in zero state") | `src/wire_allocator.jl:8-28`, `:30` |
| `resolve!` on a `ConstOperand` allocates `width` wires and emits a `NOTGate` per set bit — this is *operand materialisation*, not part of the cast | `src/lowering/operand.jl:22-43` |
| The `:trunc` source-preservation contract is a pinned test | `test/test_gboa_dirty_bit_hygiene.jl:88-120` |

---

## 2. Gate sequence as indexed pseudo-code

libcqops kernel shape (Rule 7). K5 is unary and carries **two** widths, so it does not fit
the canonical `cq_kernel_fn` parameter list at all and declares its own signature:

> **⚠ CORRECTED 2026-09-10 (`bd wf8`).** This sentence read *"K5 is unary, so the `b` operand
> is unused"* — and then printed prototypes with no `b` and two `int`s, so the sentence and
> the code described **different shapes**. That was a reasonable way to write it while Rule 7
> was still being read as a fixed parameter list. `src/kernels/cast.h:3-11` names the
> mismatch and says the printed shape is the right one; `ckd.15` (Rule 7, PRD §4) settles the
> principle — **arity and width are not part of the kernel contract, the semantics are**, and
> a kernel whose arity or operand widths differ declares its own signature and names every
> operand. M13 ships exactly the block below, reached by the test driver through a
> `cq_kd_spec.call` adapter (`tests/test_kernel_cast.c:48-61`).

```c
void cq_kernel_zext (cq_ctx*, cq_bit *dst, const cq_bit *a, int F, int T);
void cq_kernel_sext (cq_ctx*, cq_bit *dst, const cq_bit *a, int F, int T);
void cq_kernel_trunc(cq_ctx*, cq_bit *dst, const cq_bit *a, int F, int T);
/* semantics: dst ^= cast(a);  a unchanged;  zero ancillae */
```

### zext, F → T (F ≤ T)

```
n_compute = F                       /* NOT T — see the note below */

step(s), s in [0, F):
    cq_emit_cx(ctx, &a[s], &dst[s]);

/* dst[F .. T-1]: NO STEP AT ALL. Not "emit nothing" — there is no loop
   iteration. Those bits were born CQ_BIT_ZERO when dst was allocated and
   they stay CQ_BIT_ZERO: zero gates AND zero qubits, permanently. */
```

### sext, F → T (F ≤ T)

```
n_compute = T

step(s), s in [0, F):
    cq_emit_cx(ctx, &a[s], &dst[s]);          /* copy the value bits   */

step(s), s in [F, T):
    cq_emit_cx(ctx, &a[F-1], &dst[s]);        /* broadcast the sign bit */
```

Note the degenerate `F == 1` case (`sext i1 → iN`: **5 of the 15** sext pairs have an `i1`
source — `i1 → {i8,i16,i32,i64,i128}`, opcode_table.yaml:238-241,251): `a[F-1] == a[0]`, so
*every* step controls off the same single bit. The
result is `0x00…0` or `0xFF…F`, which is what C's `(int8_t)(signed)_Bool` means.

### trunc, F → T (T < F)

```
n_compute = T

step(s), s in [0, T):
    cq_emit_cx(ctx, &a[s], &dst[s]);

/* a[T .. F-1] are NEVER READ and NEVER WRITTEN. See §5 item 6 —
   they stay owned by `a`'s handle. K5 must NOT free them. */
```

### Copy-out steps

**None.** There is no scratch region and therefore no copy-out phase and no reverse phase.
The CX *is* the `^=`. `n_copyout = 0`, `n_reverse = 0`.

### I6 statement

> **I6** — inside a `cq_sandwich` compute half, every gate **target** is a bit of the scratch
> region.

K5 does not run under `cq_sandwich`, so I6 is **vacuously satisfied** (there is no compute
half). The property that actually matters here, and which K5 does satisfy, is the *reason*
I6 exists:

- **Every gate target is a `dst` bit.** All four `push!` sites in arith.jl:543/545/546/560
  target `r[i]`, never `src[i]`.
- **Every source bit appears only as a control.** Controls are never materialised (Rule 8),
  so `a` is bit-for-bit unchanged, kind-for-kind, on exit — which is exactly the kernel
  contract's "leaving `a` and `b` unchanged".
- This is enforced *by construction* by the emitter signature: `a` is `const cq_bit *`, and
  `cq_emit_cx` takes its control as `const cq_bit *`. A K5 that tried to target a source bit
  would not compile.

**No I6 violation. Not a critical finding.** K5 is the one kernel in the catalogue where the
invariant is structurally impossible to break.

---

## 3. Gate-count formula

### 3a. Derivation — counting the loops in the Julia source

Only four `push!(gates, …)` calls exist in lines 536-566, and all four push a `CNOTGate`
(verified by listing every `Gate(` occurrence in that line range: 543, 545, 546, 560). There
is **no `NOTGate` and no `ToffoliGate` anywhere in `lower_cast!`.**

| Branch | Loop(s) | Iterations |
|---|---|---|
| `:zext` (543) | `for i in 1:F` | `F` |
| `:sext` (545) | `for i in 1:F` | `F` |
| `:sext` (546) | `for i in F+1:T` | `T − F` (zero-trip when `T == F`) |
| `:trunc` (560) | `for i in 1:T` | `T` |

So: zext = `F`; sext = `F + (T − F)` = **`T`**; trunc = **`T`**.

Not counted, and deliberately so: `resolve!` at line 537 can emit up to `F` `NOTGate`s when
the operand is a `ConstOperand` (operand.jl:37-41). That is Bennett materialising a *literal*
into wires before the cast runs; it is not the cast's cost, and libcqops never pays it at all
(§5 item 2).

### 3b. Worst case — all source bits are `CQ_BIT_Q`, `dst` fresh all-`BIT_ZERO`

This is the number the L4 golden pins. **Compute half and sandwiched total are identical**
because the kernel is clean.

| | zext F→T | sext F→T | trunc F→T |
|---|---|---|---|
| NOT (`X`) — compute | 0 | 0 | 0 |
| CNOT (`CX`) — compute | `F` | `T` | `T` |
| Toffoli (`CCX`) — compute | 0 | 0 | 0 |
| **total — compute** | **`F`** | **`T`** | **`T`** |
| NOT — **sandwiched** | 0 | 0 | 0 |
| CNOT — **sandwiched** | `F` | `T` | `T` |
| Toffoli — **sandwiched** | 0 | 0 | 0 |
| **total — sandwiched** | **`F`** | **`T`** | **`T`** |

**PRD §6's `≤ T CNOT` claim holds**, and is tight for two of the three ops: zext is `F ≤ T`
(slack whenever `F < T`), sext is exactly `T`, trunc is exactly `T`.

### 3c. Realised counts — the fold table actually applied

libcqops does not pay for classical bits. Let `Q(S) ≜ #{ i ∈ S : a[i].kind == CQ_BIT_Q }`.
Forward direction (`dst` fresh, every bit `CQ_BIT_ZERO`), per PRD §3:

- `a[i] = CQ_BIT_ZERO` → `CX` with a ZERO control → **nothing**. 0 gates, 0 qubits.
- `a[i] = CQ_BIT_ONE` → `emit_x(dst[i])`; `dst[i]` is a constant → flip in place → **0 gates,
  0 qubits**; `dst[i]` becomes `CQ_BIT_ONE`.
- `a[i] = CQ_BIT_Q(q)` → target is a constant → **materialise** `dst[i]` (constant was 0, so
  **no `X`**), then `sink.cx` → **1 CX, 1 qubit**.

| | zext F→T | sext F→T | trunc F→T |
|---|---|---|---|
| `X` | 0 | 0 | 0 |
| `CX` | `Q([0,F))` | `Q([0,F)) + (T−F)·[a[F−1] is Q]` | `Q([0,T))` |
| `CCX` | 0 | 0 | 0 |
| qubits allocated | `Q([0,F))` | same as `CX` | same as `CX` |

Two L5 (classical short-circuit) cases fall straight out and should be pinned:

- **A fully-classical cast is 0 gates and 0 qubits** at every `(F, T)` and all three ops.
- **`zext i8 → i128` on a classical `i8` is literally free** — 0 gates, 0 qubits, and the
  resulting i128 register owns zero qubits (**I4**). This is the concrete mechanism behind
  PRD §1's "i128 costs nothing beyond width-genericity".

### 3d. `_unc` direction — where the `X` gates come from (Rule 14)

`_unc(out, a)` re-runs the same kernel with `dst = out`. `out` is **not** all-`BIT_ZERO`, so
the fold table takes a different row and **the forward/`_unc` counts differ**:

If `a[i]` was a classical constant at forward time (so `out[i]` is a classical constant) but
`a[i]` has since been promoted to `CQ_BIT_Q` by an in-place general `cqrt_ry` on the source
(D6: we never demote — and it is `ry` alone, not `cqrt_rz`: measured at Step 21, §7's `Rz`
constant cell does nothing at any φ, so the general `Ry` off the π-lattice is the only one
of §7's twelve cells that turns a constant into a wire), then `CX(Q, constant)` → **materialise `out[i]`, emitting `X` because
the constant was 1**, then `sink.cx`. Worst case:

| | zext F→T | sext F→T | trunc F→T |
|---|---|---|---|
| `X` — `_unc` worst case | `≤ F` | `≤ T` | `≤ T` |
| `CX` — `_unc` worst case | `≤ F` | `≤ T` | `≤ T` |
| qubits — `_unc` worst case | `≤ F` | `≤ T` | `≤ T` |

The forward emitted **zero** `X` and, in the classical case, **zero** qubits. So K5 — the
simplest kernel in the catalogue — is a clean, cheap, exhaustively-testable witness for
PRD §10 / Rule 14: **`unc == forward` is not an invariant, and asserting it is risk R6.**
The L4 test file for M13 should quote the PRD §10 note verbatim.

> **⚠ AMENDMENT 2026-09-10 (`bd wf8`) — this table is STILL A DERIVATION, and the reason is
> the mask, not neglect.** M13's `_unc` pass **is** measured, separately from the forward, and
> pinned: `run_cast_full` calls the kernel a second time on the written `dst` and
> `measure_cast` writes both rows (`tests/test_kernel_cast.c:196-202, 344-360`). But L4
> goldens are taken at the **all-quantum** mask, where nothing is left to materialise — so all
> **84** `(kernel, W)` pairs in `tests/goldens/cast.counts` have `unc == forward` and **not one
> golden row can witness the asymmetry this table predicts.** The fixture built for that
> asymmetry, `tests/test_unc_asym.inc`, covers **18 arity-2 kernels and no cast** (read
> 2026-09-10). The prediction is untouched and unobserved; do not read the equal golden rows
> as evidence against it, and do not "fix" them into an asserted equality — that is R6 by
> name.
>
> The last sentence's prescription was honoured in substance rather than in letter:
> `tests/test_kernel_cast.c:196-198` cites Rule 14 and states why the two passes are pinned
> apart, without quoting PRD §10 verbatim.

### 3e. Evaluation

Single-variable form, `W → 2W` widening and `2W → W` narrowing:

| W | zext `W→2W` (CX) | sext `W→2W` (CX) | trunc `2W→W` (CX) |
|---|---|---|---|
| 1 | 1 | 2 | 1 |
| 8 | 8 | 16 | 8 |
| 16 | 16 | 32 | 16 |
| 32 | 32 | 64 | 32 |
| 64 | 64 | 128 | 64 |

A **representative sample** of the CQ_lang cast pairs (read from
`third_party/cq_lang/opcode_table.yaml:238-324`), `X = CCX = 0` throughout, worst case (all
source bits `CQ_BIT_Q`). **This is 13 rows of a 55-pair grid, not
the whole grid** — the omitted rows (including every `i80` pair) are given in §5 item 8 and
follow the same `F` / `T` / `T` formula with no exception:

> **⚠ SOURCE CORRECTED 2026-09-10 (`bd wf8`).** The citation above read
> `/Users/sorenwilkening/Desktop/CQ_lang/tools/opcode_table.yaml:238-324` — CQ_lang's **live**
> working tree, the only copy in reach when this was written, and a Rule-1 / risk-R3 hazard
> now that a pin exists. The two are **byte-identical** — `diff -q` empty, sha256
> `6245117d…a3e82426`, re-checked 2026-09-10 against CQ_lang HEAD `170ede1` — so the line
> numbers carry over unchanged and no figure below moves; only the provenance does.

| pair | zext CX | sext CX | trunc CX |
|---|---|---|---|
| `i1 → i8` | 1 | 8 | — |
| `i1 → i64` | 1 | 64 | — |
| `i1 → i128` | 1 | 128 | — |
| `i8 → i16` | 8 | 16 | — |
| `i16 → i32` | 16 | 32 | — |
| `i32 → i64` | 32 | 64 | — |
| `i64 → i128` | 64 | 128 | — |
| `i8 → i1` | — | — | 1 |
| `i16 → i8` | — | — | 8 |
| `i32 → i16` | — | — | 16 |
| `i64 → i32` | — | — | 32 |
| `i128 → i64` | — | — | 64 |
| `i128 → i1` | — | — | 1 |

The zext column is the source width; the sext and trunc columns are the target width. The
`i1 → i128` row is the extreme: zext is 1 CX, sext is 128 CX — a **128× spread** between the
two casts of the same pair. Any L4 golden that treats "cast" as one cost is wrong.

> **✅ AMENDMENT 2026-09-10 (`bd wf8`) — §3e checked against the goldens, figure by figure.**
> The tables above were derived from loop bounds and were unobserved when written; M13 has
> since been built and pinned. Nothing above is rewritten — this is what the comparison
> found, run on 2026-09-10 against `tests/goldens/cast.counts` (168 data rows) and against
> `build-release/tests/test_kernel_cast` (10/10 green; also green in Debug).
>
> | §3e figure | inferred | golden row | |
> |---|---|---|---|
> | 13-row sample table, all 20 CX figures | `F` / `T` / `T` | `zext_f1@8=1`, `sext_f1@8=8`, `zext_f1@64=1`, `sext_f1@64=64`, `zext_f1@128=1`, `sext_f1@128=128`, `zext_f8@16=8`, `sext_f8@16=16`, `zext_f16@32=16`, `sext_f16@32=32`, `zext_f32@64=32`, `sext_f32@64=64`, `zext_f64@128=64`, `sext_f64@128=128`, `trunc_f8@1=1`, `trunc_f16@8=8`, `trunc_f32@16=16`, `trunc_f64@32=32`, `trunc_f128@64=64`, `trunc_f128@1=1` | **MATCH**, 20/20 |
> | `W → 2W` table, `W ∈ {8,16,32,64}` (12 figures) | `W` / `2W` / `W` | same rows as above | **MATCH**, 12/12 |
> | `W → 2W` table, the `W = 1` row (3 figures) | 1 / 2 / 1 | — | **UNPINNED**: `W = 2` is not on the goldens' ladder `{1, 8, 16, 32, 64, 80, 128}`, so `zext i1→i2` / `sext i1→i2` / `trunc i2→i1` have no golden. `i2` is not a CQ_lang width either |
> | §3b, every `X` and `CCX` cell | 0 | all **168** golden rows carry `0` in both columns | **MATCH** |
> | §3c at the all-quantum mask (`Q([0,F)) = F`, `Q([0,F))+(T−F) = T`, `Q([0,T)) = T`) | — | the goldens are taken at that mask (their `measured-at:` line) | **MATCH** |
> | §3c's qubit row, "qubits allocated = CX" | `Q([0,F))` | `zext_allocates_only_the_source_width`: `zext i8→i64` owns **8** qubits and moves `peak` by **8** | **MATCH** |
> | §3d's `_unc` worst case | `≤ F` / `≤ T` X gates | — | **NOT WITNESSED** — see the amendment on §3d |
>
> The document's own §3e header calls this "13 rows of a 55-pair grid". The suite sweeps the
> **28 ordered pairs per op** that the seven-value ladder admits, not the 55 shipped pairs;
> what makes that sufficient is I5 — `cast.c` has no width switch — and it is stated as a
> limit, not waved away.

---

## 4. Ancilla count

**Zero. `ancillae(F, T) = 0`, unconditionally** — for all three ops, all `(F, T)`, all
bit-kind masks, both the forward and the `_unc` direction.

There is nothing to return to `|0⟩` because nothing is taken from the pool as scratch.
Bennett's single `allocate!(wa, T)` (arith.jl:540) allocates the *result* `r`, which is
`vw[inst.dest]` (line 565) — the SSA value, not scratch; in libcqops that vector is `dst`,
supplied by the caller. Qubits *are* taken from the pool during the kernel, but only via
`cq_materialise` on `dst` bits (Rule 5), and those belong to `dst`, whose lifetime CQ_lang
owns. K5 itself allocates and frees nothing.

(The one shared control ancilla that `cq_ctrl_push` may lazily acquire belongs to the emitter
mode — Rule 9 — not to K5. No kernel is aware the controlled axis exists.)

---

## 5. Deltas from upstream

1. **`dst =` vs `dst ^=`.** Bennett allocates the result inside `lower_cast!` and *defines*
   it (`vw[inst.dest] = r`, line 565). libcqops takes `dst` from the caller and XORs into it.
   On a fresh all-`BIT_ZERO` `dst` the two coincide (`0 ^ x = x`), which is why the gate
   sequence ports verbatim. They diverge on `_unc` — §3d — and that divergence is the point
   of the contract, not a defect.

2. **Constant operands never materialise.** Bennett's `resolve!` turns a `ConstOperand` into
   real wires plus one `NOTGate` per set bit (operand.jl:30-42). libcqops folds the constant
   through the CX at emission and allocates nothing (PRD §3). So the realised libcqops count
   is always `≤` Bennett's for the same cast, often 0.

3. **Do not port `_fold_constants`.** Bennett recovers the same savings *after* emission, via
   a gate-level pass (`src/lowering/driver.jl:407+`, default-on since Bennett-epwy / U28) that
   tracks known wire values and rewrites `CNOT(known-0, …)` → nothing, `CNOT(known-1, t)` →
   `NOT(t)`, etc. That is an optimiser. libcqops gets the identical result *structurally*, at
   emission, from the fold table — and v1 forbids gate-level optimisation outright. Same
   outcome, different timing, and ours cannot drift out of sync with the shadow.

4. **zext's high bits: wires vs constants.** Bennett leaves `r[F+1..T]` as physically
   allocated, untouched zero **wires** — real qubits in the count. libcqops leaves them as
   `CQ_BIT_ZERO` **constants**: zero qubits (I4). This is the single largest representational
   win in K5 and it is what makes wide zext free.

5. **No width guard upstream; libcqops must add one.** Neither `lower_cast!` nor the `IRCast`
   constructor (ir_types.jl:206-215) asserts `F ≤ T` for zext/sext or `T ≤ F` for trunc. A
   `:zext` with `T < F` walks `r[i]` past its end — in Julia that is a `BoundsError`; in C it
   is a **buffer overrun**. M13 must assert the width relation explicitly and fail loud.
   (Bennett's fp-cast fallbacks even encode `T == F` as `:trunc`, instructions.jl:7662/7685,
   which lowers to an `F`-CNOT identity copy. CQ_lang's cast grid contains no same-width pair
   — verified in §5 item 8 — so libcqops need not support that shape, but should reject it
   loudly rather than silently copy.)

   > **✅ HALF DISCHARGED, HALF DECLINED — 2026-09-10 (`bd wf8`).** *Discharged:* the width
   > guard exists and is a **hard error in both configurations**, not a Debug assert —
   > `check_widths`, `src/kernels/cast.c:21-33`, with the reason spelled out there (Step 24
   > runs under Release, where a Debug-gated assert is simply absent). Three death cases were
   > green on 2026-09-10: `test_kernel_step11_death.a_widening_cast_that_narrows`,
   > `.a_narrowing_cast_that_widens`, `.a_sign_extension_that_narrows`.
   > *Declined:* the parenthesis's closing recommendation — **"should reject `T == F` loudly
   > rather than silently copy" was NOT adopted.** M13 **accepts** `T == F` as an identity
   > copy: `sext`'s broadcast loop is zero-trip there and `zext`/`trunc` degenerate to a plain
   > `F`-CX copy, so the construction needs no special case. `cast.c:15-20` records that as a
   > decision and says in terms that this document "picked it in passing without flagging it as
   > one". Both readings are defensible and the shape is unreachable from CQ_lang's shipped
   > grid; the pinned goldens carry the accepted behaviour explicitly — `zext_f8 forward 8 → 8
   > CX`, `trunc_f128 forward 128 → 128 CX`, and nineteen more same-width pairs — **21 in all,
   > seven widths × three ops**, 42 rows counting forward and `_unc` separately (counted
   > 2026-09-10 over `tests/goldens/cast.counts`). **A reader acting on the sentence above
   > would write a refusal the shipped kernel does not have.**

6. **trunc's dropped bits — no leak, and the reason is ownership, not luck.** Bennett
   (arith.jl:548-559) says outright that `src[T+1..F]` are left at their SSA values and that
   the *global* reverse pass cleans up whatever produced them. libcqops has no global reverse
   pass, and does not need one here, because **the dropped bits are not ancillae — they are
   bits of `a`**, the caller's source register. The kernel contract requires `a` be left
   unchanged; K5 simply never reads or writes them; the qubits behind them stay owned by
   `a`'s handle. CQ_lang decides when `a` dies, by emitting `cqrt_free(a)` (which asserts
   provable-zero, Rule 6) or by `_unc`-ing `a` from *its* own sources first. That is above our
   box. Two hard prohibitions follow:

   - **K5 must not free `a`'s high bits.** They hold the discarded high half of the value and
     are not provably `|0⟩`. Freeing them is precisely the silent-state-collapse signature
     Rule 6 exists to catch.
   - **K5 must not "optimise" trunc into an alias/move** that reparents `a`'s low `T` qubits
     into `dst`. That violates I2 (no qubit index in two live registers) and makes the next
     `cqrt_free` on either handle a double-free of a rail that is not provably zero. **Copies
     are physical, never aliases** (Rule 5 / I2). The temptation is real here — trunc *looks*
     like a pure slice — which is why it is written down.

7. **Free-list order.** Bennett's `WireAllocator` free list is sorted **descending** and
   `pop!` returns the **minimum** index (wire_allocator.jl:3, 19-21) — lowest-index-first.
   libcqops D4 is **LIFO**. Same gate counts; different qubit indices in a trace. Irrelevant
   to L4 (counts), relevant to L6 (trace goldens) — which is exactly why kernel goldens pin
   counts and not traces.

8. **⚠ The width grid in the CQ_lang table is wider than this task's brief and than the
   PRD *was*.** Read from `third_party/cq_lang/opcode_table.yaml` (lines 232-324), the
   `int_width` cast pairs are:

   > **⚠ SOURCE CORRECTED 2026-09-10 (`bd wf8`).** This item read *"the width grid in the
   > **live** CQ_lang table"*, read from
   > `/Users/sorenwilkening/Desktop/CQ_lang/tools/opcode_table.yaml`. That was the only copy
   > available when it was written — Step 0 had not run — and it is what the third bullet
   > below was hedging about. The pin now exists, the path above is it, and the two are
   > byte-identical, so **no figure in this item moves**; only its provenance does.

   | opcode | pairs | grid |
   |---|---|---|
   | `sext` | **15** | `{i1,i8,i16,i32,i64} → {i8,i16,i32,i64,i128}`, strictly widening. **No i80.** |
   | `zext` | **20** | the same 15, **plus `{i1,i8,i16,i32,i64} → i80`** |
   | `trunc` | **20** | `{i128,i64,i32,i16,i8} → {i1,i8,i16,i32,i64}`, strictly narrowing, **plus `i80 → {i1,i8,i16,i32,i64}`** |

   > **✅ RE-COUNTED AGAINST THE PIN 2026-09-10 (`bd wf8`), mechanically, not by eye.** An
   > `awk` pass over `third_party/cq_lang/opcode_table.yaml` that walks every `pairs:` block
   > under a `kind: int_width` opcode and tallies the `{ from:, to: }` entries returns
   > **`trunc` 20, `sext` 15, `zext` 20 — 55 int_width cast pairs**, and on the same pass:
   > `i80` appears as a **source** 5 times (all `trunc`) and as a **destination** 5 times (all
   > `zext`), never in `sext`, never as a `trunc` destination, never as a `zext` source;
   > `i128` appears as a `sext` and `zext` destination 5 times each and as a `trunc` source 5
   > times, never the other way; and **no pair has `from == to`**. Every clause of this item's
   > table and of its first bullet reproduces exactly. Line citations re-resolved against the
   > pin the same day: `:231` is still the "3 kinds x 10 pairs each = 30 pairs" comment, `:21`
   > still "9 kinds, 90 pairs", `:236` / `:260` / `:295` still `variants: [fwd, inv, unc]`.

   Three things follow. **⚠ 2026-09-10 (`bd wf8`): this read "none of which I am authorised
   to settle here", and that is now true of only the first and third.** Both of those were
   open questions and both have since been settled elsewhere — the notes below say where. The
   middle one was never a question: it is an observation about CQ_lang's own prose, it needed
   no authority, and it still holds against the pin.

   - **`i80` is in the cast grid and is not in the PRD.** *(**⚠ SECOND HALF CORRECTED
     2026-09-10, `bd wf8`: `i80` IS in the PRD now.** It was not when this was written, which
     is why the bullet is phrased as a discrepancy. `PRD-v1.md` §1 carries the
     **i80-is-IN-scope** decision of 2026-08-14 outright, and names this very pair of shapes —
     "`trunc`-from-i80 and `zext`-to-i80: pure classical permutations already inside v1's
     kernel set". PRD-v1.md §13's delivery-increment row 8 and PRD §15 D8's i80 worked
     example carry it too. So this is no longer a discrepancy in either direction; the
     paragraph is kept because the **grid facts** it establishes are still the ones a reader
     needs.)* The table's own comments say i80 is
     a `zext` **destination only** and a `trunc` **source only** (never a `sext` operand,
     never a `zext` source, never a `trunc` destination) — it exists to serve the
     `promoteFp80Union` RMW field-insert/extract idiom (bd 3c6m.51 Inc 2). K5's construction
     is width-generic and handles `T = 80` / `F = 80` with no change whatsoever, so this costs
     M13 nothing. But it does mean the K5 symbol grid is **55 pairs, not the 30 the table's
     own stale header comment claims** ("Integer width casts (3 kinds x 10 pairs each = 30
     pairs)", line 231), and it bore on **Step 0.5 blocker #1**, then framed as ~~1455 vs
     1474~~. **RESOLVED 2026-08-14 by recounting the whole table, exactly as this note
     said to** (re-verified 2026-08-22 against CQ_lang's own generated
     `runtime/cq_templates.h`, an exact 2479-name set match): the grid is **2479 = 1595
     purely-integer + 884 fp-touching**; `1474` is a **phantom** matching no revision and
     no partition. See PRD §1 and `docs/cqrt_census.txt` Part F — not this doc. The **55
     int-only cast pairs** counted here are CORRECT (sext 15 + zext 20 + trunc 20).
     **Re-verified a third time 2026-09-10 (`bd wf8`), this time against the PIN rather than
     against a live tree or a generated header** — the mechanical `awk` re-count above
     returns 15 / 20 / 20 = 55 with no exception.
   - **The table's header comments are stale relative to its own data.** Line 231 says 30
     int-width pairs; the lists below it contain 55. Line 21 says "9 kinds, 90 pairs" for all
     casts. Trust the `pairs:` lists, not the prose.

     > **✅ RE-MEASURED AGAINST THE PIN 2026-09-10 (`bd wf8`) — both halves hold, and the
     > second is worse than stated.** `:231` and `:21` still carry those words verbatim. The
     > same `awk` pass, run over **every** `pairs:` block rather than the `int_width` ones,
     > returns **10 cast opcodes and 147 pairs** — `sext` 15, `zext` 20, `trunc` 20,
     > `fpext` 6, `fptrunc` 6, `fptosi` 16, `fptoui` 20, `sitofp` 16, `uitofp` 20,
     > `bitcast` 8 — against the header's "9 kinds, 90 pairs". This bullet's advice is
     > therefore unchanged and its numbers are now measured rather than eyeballed. It bears
     > on nothing K5 computes; the fp and cross-domain rows are v2 (PRD §1).
   - **This repo had no pinned copy of `opcode_table.yaml` when this was written** (Step 0
     had not run). **It does now** — `third_party/cq_lang/opcode_table.yaml` @ CQ_lang
     `a6a92fe`, sha256 `6245117d…a3e82426`, and that is what to read: reading CQ_lang's live
     tree instead is a Rule-1 / risk-R3 hazard, since the corpus moves and the pin does not.

     > **✅ NOW A VERIFICATION — CORRECTED 2026-09-10 (`bd wf8`).** This bullet ended: *"At the
     > time, I read CQ_lang's live working tree, which may be ahead of whatever revision
     > PRD-v1.md was written against. That is the most likely explanation for the i80
     > discrepancy — but it is an explanation, not a verification."* That was the honest thing
     > to write with no pin on disk. **It is a verification now, and the explanation it
     > offered was not the right one.**
     >
     > Measured 2026-09-10, four commands:
     >
     > | | |
     > |---|---|
     > | `shasum -a 256 third_party/cq_lang/opcode_table.yaml` | `6245117dcffb4b1c34935ac9ba3f9d42d754c2dc5a881c9510ad594ba3e82426` |
     > | `diff -q` pin vs `/Users/sorenwilkening/Desktop/CQ_lang/tools/opcode_table.yaml` | **empty — byte-identical**, and the live file's sha256 is the same string |
     > | `git -C …/CQ_lang rev-parse --short HEAD` | **`170ede1`** — CQ_lang's HEAD has moved well past the pin's `a6a92fe`, exactly as `CLAUDE.md` warns |
     > | `git -C …/CQ_lang log -1 -- tools/opcode_table.yaml` | **`4d5ba4e`, 2026-07-15** — the yaml has not been touched since, which is *why* pin and live tree still agree |
     >
     > So the i80 rows in this item's table **are the pinned ABI**, not an artefact of reading
     > a live tree, and the discrepancy this document flagged was **real** — the live tree was
     > not ahead of the PRD on this point; the PRD simply did not carry i80 yet. The
     > `a6a92fe` → `170ede1` gap is the reason the *yaml sha256* rather than the checkout is
     > what proves anything here.

9. **The cast grid carries `variants: [fwd, inv, unc]` — no `controlled`.** Every cast pair
   declares three variants (opcode_table.yaml:236, 260, 295), versus the ten-variant
   `fwd_qq/fwd_hl/inv_*/controlled_*/unc_*` grid the binary ops carry (line 184). The table's
   naming header (lines 35-52) confirms "Controlled axis is BINARY-ops-only". PRD §10 says
   CQ_lang's spine no longer emits `_inv` for data templates, so `cq_template_<op>_<from_to>_inv`
   is a **link-surface symbol** the shim must emit but the spine should never call; per the
   compare precedent it would be "allocate a fresh rail and run the forward". `gen_shim.py`
   (M27) needs to emit it either way. Flagging it because K5 is a *unary cast*, and the
   `_hl`/`_lh` shape tokens that dominate the binary grid do not apply here at all — the
   uncompute spelling is the bare `cq_template_<op>_<from_to>_unc(out, src)` (line 54).

   > **⚠ CORRECTED 2026-09-10 (`bd wf8`) — the guessed body is WRONG, and the guess was the
   > reasonable one at the time.** Everything else in this item stands: the cast grid does
   > carry `variants: [fwd, inv, unc]` and no `controlled` (re-checked against the pin at
   > `:236`, `:260`, `:295`), M27 does have to emit the symbol, and the `_unc` spelling is as
   > printed. What is now settled the other way is the **body**. **PRD §15 D14** (resolved
   > 2026-08-21) finds that `_inv` on the `cq_template_*` DATA grid means `f⁻¹`, not "run the
   > forward on a fresh rail" — and that `f⁻¹` **does not exist** for the non-injective
   > opcodes, naming **`trunc`** among them. There is therefore no uniform body, in v1 or any
   > version, and v1 gives all **603** purely-integer `_inv` symbols a **loud abort naming the
   > symbol**. Reading the compare `_inv` across by analogy was the available move with no
   > cast `_inv` specification on disk; it produced the wrong answer, and a reader acting on
   > it would implement a body D14 forbids.

10. **Possible shadow-poison interaction with `_unc` — INFERRED at the time, then FILED,
    MEASURED and SETTLED: it is `PRD §15 D15` (2026-08-22).** The inference below was
    correct as far as it went and its remedy is not the one it implies: the free-time
    evidence is no longer the shadow, so a rail in this state is not hard-errored on the
    shadow's say-so, and a rail no evidence can discharge is **stranded** rather than
    aborted. Read D15 §3 for what that disposition is; the paragraph below is kept as
    the original observation.

    §3d's `_unc` scenario requires the source to have been rotated in-place between forward and
    uncompute. PRD §3's shadow rule poisons a bit to *unknown* on any `Ry`/`Rz` outside the
    classical angle set, and poison is sticky. If CQ_lang restores the source's state with an
    inverse rotation, our shadow stays *unknown* even though the state is determinate — the
    sanctioned direction of conservatism — but then `_unc` leaves `out[i]` as a qubit whose
    shadow is not known-zero, and the following `cqrt_free(out)` **hard-errors on a rail that
    is actually clean**. I did not verify this: it depends on shadow semantics that are not
    implemented yet and on CQ_lang restore behaviour I did not read. It is not K5-specific —
    it would hit every kernel. Noting it here because K5 is the smallest place to reproduce it.

---

## 6. Confidence

> ### ✅ EXECUTED 2026-09-10 — read this before the "Inferred, not read out of an
> ### implementation" list below and before the closing
> ### "UNVERIFIED-BY-EXECUTION" paragraph. Both were true when written; both are
> ### now stale for the libcqops half and **still exactly right for the Julia half**.
> ### (`bd wf8` / `bd 18f`; the shape is K09.md §6's banner, deliberately.)
>
> **M13 is on disk** — `src/kernels/cast.c` (67 lines) and `src/kernels/cast.h` — and
> `tests/test_kernel_cast.c` is green in **both** configurations: **10/10 cases**, run
> directly out of `build-release/tests/test_kernel_cast` and
> `build-debug/tests/test_kernel_cast` on 2026-09-10 (no rebuild; the binaries are newer
> than every cast source and golden). What that run measured, against a `cq_sink` counter
> and the shared kernel driver rather than against this document:
>
> - **§3b's and §3e's closed forms `F` / `T` / `T`, as full `(X, CX, CCX)` tuples rather
>   than as totals**, at every ordered pair the shipped ladder `{1, 8, 16, 32, 64, 80, 128}`
>   admits — 28 pairs per op, **84 `(kernel, W)` rows**, forward and `_unc` pinned
>   **separately**, in `tests/goldens/cast.counts` (168 data rows). `measure_cast` asserts
>   the *measured* `X` and `CCX` columns are zero rather than comparing a literal 0 against
>   a literal 0 — the regression that file's own comment records — and asserts
>   `ry + rz + mz == 0` on both passes. The figure-by-figure comparison is tabulated under
>   §3e; the short form is **20/20 and 12/12 MATCH, 3 unpinned, 1 not witnessed**.
> - **§3c's realised counts**, which is what the goldens *are*: they are taken at the
>   all-quantum mask (their `measured-at:` line says so), where §3c reduces to
>   `Q([0,F)) = F`, `Q([0,F)) + (T−F) = T`, `Q([0,T)) = T`, `X = CCX = 0`.
> - **§2's gate sequences produce the right VALUES**, differentially against a two-word
>   reference model that reaches i80 and i128 — `k5_zext_sweep` / `k5_sext_sweep` /
>   `k5_trunc_sweep` walk 28 width pairs each, and `controlled` re-walks the narrow pairs
>   under each of PRD §9's four regions: **156 sweep invocations, 4,992 sampled L1 cases**
>   in the release run, each carrying L2 and L3. Plus four by-hand cases at the widths a
>   one-word reference cannot reach — `wide_sext_fills_the_high_word`,
>   `wide_i80_crosses_the_word_seam`, `sext_against_an_independently_derived_oracle`,
>   `the_two_reference_models_agree_below_64`.
> - **§3c's two L5 bullets.** The driver asserts **zero gates and zero qubits** at the
>   all-classical mask on every sweep case under §9's three classical control modes (and
>   asserts the *correct non-zero* claim under a quantum control, which is not this
>   document's subject). The all-classical mask is a **forced anchor** of every sample
>   budget, so `zext i8 → i128` on a classical `i8` is covered as a case rather than by
>   extrapolation from the formula.
> - **§4's `ancillae = 0`, and §5 delta 4's representational win**, as a pool high-water
>   mark: `zext_allocates_only_the_source_width` runs `zext i8 → i64` and asserts `dst`
>   owns **8** qubits and not 64, and that `peak` moved by exactly 8 — no transient.
> - **§2's "every source appears only as a control"**, in value *and* in kind: the driver's
>   `check_source` runs after the forward with kinds included, on every case.
> - **§5 item 5's width guard**, discharged in both configurations — three death cases
>   green. Its `T == F` recommendation was **declined**; the amendment on item 5 says so.
> - **§5 item 8's grid, re-counted mechanically against the PIN**, and the pin proved
>   byte-identical to CQ_lang's live tree at HEAD `170ede1`. Amendments on that item.
>
> **What was NOT executed, and what is not covered.**
>
> - **Julia. Still not installed** — `which julia` returns nothing, 2026-09-10 — so no
>   Bennett code was run here either. §3a's per-loop `push!` counts remain a
>   **transcription** of `arith.jl`, and the closing paragraph of this section stands
>   verbatim for the upstream half. Re-read against the pinned snapshot on 2026-09-10: the
>   four `push!` sites are still `543 / 545 / 546 / 560`, all `CNOTGate`, `lower_cast!`
>   still opens at `:536` and `vw[inst.dest] = r` is still at `:565`; `BASELINES.md` still
>   records **no** upstream baseline for K5, so there is still no published per-kernel
>   figure for `lower_cast!` to check against.
> - **§3d's `_unc` worst-case table.** Measured `_unc` counts exist, but only at the mask
>   where they cannot differ. See the amendment there — this is the one numeric claim in
>   the document that execution has *not* reached.
> - **The 55-pair symbol grid, pair by pair.** The suite drives 28 ordered pairs per op off
>   a 7-value ladder; the shipped grid is 55 pairs. Every *width* in the grid is on the
>   ladder and `cast.c` has no width switch (I5), which is the argument — not a measurement.
> - **Nothing about the shim.** §5 item 9 concerns `cq_template_*_inv`; no claim here was
>   checked against M26/M27/M28.
>
**Verified by reading source** (all paths relative to `third_party/bennett/` at pinned commit
980805de8531…):

- The full text of `lower_cast!`, `src/lowering/arith.jl:534-566`. Quoted verbatim in §1.
- That the function contains exactly four gate emissions, all `CNOTGate`, at lines 543, 545,
  546, 560 — listed every `Gate(` occurrence in the range. **Zero `NOTGate`, zero
  `ToffoliGate`.** The loop bounds (`1:F`, `1:F`, `F+1:T`, `1:T`) are read off those lines, so
  the counts `F` / `T` / `T` in §3 are arithmetic over source I read, not recall.
- That every gate *target* is `r[i]` and every *source* appears only as a control — hence
  clean, hence no sandwich, hence ancillae = 0.
- That `r = allocate!(wa, T)` is the result (`vw[inst.dest] = r`, line 565), not scratch.
- `allocate!` / `free!` / free-list ordering: `src/wire_allocator.jl:1-52`.
- `IRCast` field list and the *absence* of any `F`-vs-`T` relation check:
  `src/ir_types.jl:11, 200-216`.
- Single dispatch site: `src/lowering/types.jl:253-254`.
- `ConstOperand` materialisation emitting `NOTGate`s: `src/lowering/operand.jl:22-43`.
- `_fold_constants` being a post-hoc gate-level pass: `src/lowering/driver.jl:395-454`.
- The `:trunc` source-preservation contract test: `test/test_gboa_dirty_bit_hygiene.jl:88-120`.
- CQ_lang cast pair grid, pair counts (15/20/20), the i80 restriction comments, and
  `variants: [fwd, inv, unc]`: `third_party/cq_lang/opcode_table.yaml:225-324` and the naming
  header at `:20-60`. Pair counts were obtained by parsing the `pairs:` blocks, not by eye.
  **⚠ SOURCE CORRECTED 2026-09-10 (`bd wf8`):** this line cited
  `/Users/sorenwilkening/Desktop/CQ_lang/tools/opcode_table.yaml:225-324` — the live tree,
  the only copy on disk when it was written. The pin is byte-identical, so every line number
  resolves unchanged and no figure moves; see §5 item 8's third bullet for the four commands.
  Re-parsed mechanically against the pin on 2026-09-10 and the counts reproduce.
- PRD claims checked against the source rather than quoted: §5's clean list, §6's K5 row
  (`✔`, `≤ T CNOT`), §3's fold table, §10's `_unc` / `_inv` text, plan §3's M13 row.

**Inferred, not read out of an implementation** — **⚠ HEADER CORRECTED 2026-09-10
(`bd wf8`).** This parenthesis read *"(nothing is built — CLAUDE.md's "NOTHING IS BUILT YET"
still holds; there is no `src/`, no `include/`, no `tests/`)"*. **That was true when written
and is flatly false now**, and it is the more dangerous of this document's two stale
self-assessments, because a reader who believes it will re-derive figures that are pinned on
disk. All three directories exist; Layers 0–4 are complete; K5 itself is `src/kernels/cast.c`,
`tests/test_kernel_cast.c` and `tests/goldens/cast.counts`. The banner at the head of this
section says what a 2026-09-10 run measured. The list below is kept **verbatim**, each item
followed by a dated verdict on what has since happened to it — it is the record of what was
inference at the time, and three of its four items have since been settled by something other
than inference:

- Every number in §3c and §3d. These apply the PRD §3 fold table by hand to the ported gate
  sequence. The fold table is a *specification*, so these are derivations from a document, not
  observations of behaviour. They are the numbers L4 should pin, and L4 is what will confirm
  them.

  > **✅ §3c MEASURED / ⚠ §3d STILL INFERRED — 2026-09-10 (`bd wf8`).** This item covers two
  > sections and they have come apart. **§3c:** the fold table is no longer only a
  > specification, it is code, and §3c's cells are confirmed at the all-quantum mask by
  > `tests/goldens/cast.counts` and at the all-classical mask by the driver's L5 assertions —
  > *"L4 is what will confirm them"* discharged exactly as written. **§3d:** still a
  > derivation from a document, because L4 pins `_unc` at the all-quantum mask where it cannot
  > differ from the forward, and `tests/test_unc_asym.inc` carries no cast. Amendment under
  > §3d.

- The claim that `_unc` can emit up to `T` `X` gates where the forward emitted zero. It
  follows from PRD §10 + D6 + the fold table's "materialise `t`, emit `X` if the constant was
  1" row. Sound as a derivation; unobserved.

  > **⚠ STILL UNOBSERVED — 2026-09-10 (`bd wf8`).** The one numeric claim in this document
  > that execution has not reached. Nothing measured contradicts it and nothing measured
  > supports it; see the amendment under §3d for why the goldens structurally cannot.

- §5 item 10 in its entirety. Explicitly speculative and labelled as such.

  > **✅ SETTLED ELSEWHERE, 2026-08-22 — noted here 2026-09-10 (`bd wf8`).** It was right as
  > far as it went. It is now **PRD §15 D15**: the free-time evidence is no longer the shadow,
  > and a rail no evidence can discharge is **stranded** rather than aborted. Item 10 carries
  > the pointer.

- The reading that `_inv` for a cast means "allocate a fresh rail and run the forward" — that
  is PRD §10's description of the *compare* `_inv`, applied by analogy. I did not find a cast
  `_inv` specification anywhere.

  > **❌ SETTLED THE OTHER WAY, 2026-08-21 — noted here 2026-09-10 (`bd wf8`).** The absence
  > this item reports is real; the analogy drawn from it is not. **PRD §15 D14** finds `_inv`
  > on the data grid is `f⁻¹`, that `f⁻¹` does not exist for `trunc`, and that v1 gives every
  > one of the 603 purely-integer `_inv` symbols a loud abort. The amendment on §5 item 9 has
  > it.

**UNVERIFIED-BY-EXECUTION — KEPT VERBATIM, AND NOW SCOPED TO THE UPSTREAM HALF
(2026-09-10, `bd wf8`).** Re-checked sentence by sentence on 2026-09-10. Every claim about
**Julia and Bennett** is **still true** — `which julia` returns nothing, no Bennett code was
run, `gate_count()` / `simulate()` / `verify_reversibility()` were never called, and
`BASELINES.md` still carries no K5 baseline to cross-check against. **One sentence is now too
broad and is corrected here rather than in place:** *"Every figure is arithmetic over loop
bounds I read"* was true of the whole document when written and is now true only of §3a and
§3d — §3b, §3c and §3e have been measured, and the banner at the head of this section says by
what. The paragraph is otherwise not rewritten and must not be: read it as the **upstream**
row of that banner, not as a statement about M13.

Julia is not installed on this machine; no Bennett code was run.
No gate count in this document was produced by executing `gate_count()`, `simulate()`, or
`verify_reversibility()`. Every figure is arithmetic over loop bounds I read. In particular I
did **not** cross-check against `BENCHMARKS.md` or `test/test_gate_count_regression.jl` — and
deliberately so: those pin *full-wrap* totals for whole compiled functions, not the
compute-half count of a single `lower_*!`, so agreement would be meaningless and disagreement
would be uninformative. I found no per-kernel published figure for `lower_cast!` to check
against; if one exists I did not locate it.

**⚠ CORRECTED 2026-09-10 (`bd wf8`).** This document closed: *"Nothing in this document is a
verification claim about a test suite. No test was run."* True on the day it was written —
there was no suite — and **false now**. The banner at the head of §6 is exactly such a claim,
and it is deliberately literal per Rule 17: `test_kernel_cast` (10/10) plus four
`test_kernel_step11_death` cast cases were run on 2026-09-10, out of the already-built
`build-release/` and `build-debug/` trees, and nothing else in this repo's suite was. **No
`ctest` run, no rebuild, and no claim here about any layer other than the ones the banner
names.** The Julia sentence above it is untouched and still stands.
